source "https://rubygems.org"

# Local Jekyll build (use this to run `bundle exec jekyll serve` locally)
gem "jekyll", "~> 4.2"
gem "jekyll-theme-simplex"

# If you're using Ruby >= 3.0, add webrick for the built-in server
gem "webrick", "~> 1.7"
